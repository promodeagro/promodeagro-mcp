# Example environment configuration
# Copy this to .env.dev, .env.stage, or .env.prod and customize

CDK_ENV=dev
PROJECT_NAME=alert-engine
BASE_DOMAIN=totogicore.com

# AWS Configuration
AWS_PROFILE=your-aws-profile
CDK_DEFAULT_REGION=us-east-1
CDK_DEFAULT_ACCOUNT=your-account-id

# Database Configuration (Aurora DSQL - optional)
# DB_CLUSTER_ID=your-aurora-cluster-id
# DB_CLUSTER_ENDPOINT=your-aurora-endpoint  
# DB_USERNAME=postgres

# Application Configuration
MCP_SERVER_PORT=8000
MCP_SERVER_HOST=0.0.0.0
LOG_LEVEL=INFO

# Secrets ARN (update with your actual secrets ARN)
SECRETS_ARN=arn:aws:secretsmanager:region:account:secret:name
